---
description: Show Claude Code costs per git branch / PR
argument-hint: "[branch-name] (omit to see all branches)"
allowed-tools:
  - Bash
---

# claudetop-branch

Show cost analysis by git branch. Use to see how much a feature branch or PR cost across all sessions.

```bash
claudetop-stats branch $ARGUMENTS
```

Run the command and display the full output.
